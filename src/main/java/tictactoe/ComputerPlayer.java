package tictactoe;

// Define the ComputerPlayer class, a subclass of Player, representing a computer player
class ComputerPlayer extends Player {

    public ComputerPlayer(char symbol) {
        super(symbol);
    }
}
